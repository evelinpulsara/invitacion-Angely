import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // No necesitas configurar webpack para imágenes estáticas
  // Solo asegúrate de que tus imágenes están en la carpeta 'public'
};

export default nextConfig;